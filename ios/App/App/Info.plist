<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>ShiftTrack</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>

	<!-- ===================================================================
	     LOCATION
	     Purpose strings are shown verbatim in the system prompt and are
	     read by App Review. State the business justification explicitly;
	     vague strings are a common rejection reason for Always access.
	     =================================================================== -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>ShiftTrack uses your location for verifying clock-in at the store.</string>

	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>ShiftTrack uses your location for measuring delivery mileage for pay, and showing managers where drivers are, only while clocked in on a delivery shift. Tracking stops the moment you clock out.</string>

	<!-- Legacy key. iOS 10 and earlier only; harmless to retain and some
	     tooling still checks for it when Always access is requested. -->
	<key>NSLocationAlwaysUsageDescription</key>
	<string>ShiftTrack uses your location for measuring delivery mileage for pay, and showing managers where drivers are, only while clocked in on a delivery shift. Tracking stops the moment you clock out.</string>

	<!-- Required for CLLocationManager to deliver updates once suspended.
	     Enable the matching "Location updates" capability in Xcode under
	     Signing &amp; Capabilities > Background Modes, or the entitlement
	     will not be applied to the built target.

	     showsBackgroundLocationIndicator (the blue status-bar pill
	     reviewers look for) is NOT an Info.plist key -- it's a
	     CLLocationManager property, and @capacitor-community/background-
	     geolocation's iOS side sets it automatically whenever a watcher is
	     started with a backgroundMessage (see its Plugin.swift:
	     `manager.showsBackgroundLocationIndicator = background`). Nothing
	     to configure here beyond this array existing. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
	</array>
</dict>
</plist>
